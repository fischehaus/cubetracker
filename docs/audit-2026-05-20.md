# Cubetracker Claude-Code-Setup Audit — 2026-05-20

**Ziel:** Abgleich unseres `.claude/`-Setups + `CLAUDE.md` mit den offiziellen
Anthropic-Docs (code.claude.com/docs). Konkrete Vorschlaege für Verbesserungen,
priorisiert nach Effort × Mehrwert.

**Methodik:** Sub-Agent-Fanout (1 Agent pro Doku-Sektion, parallel),
strukturiertes 5-Felder-Template, Synthese im Hauptchat.

---

## Probelauf-Scope (4 Sektionen)

1. **Hooks** (`hooks.md` + `hooks-guide.md`)
2. **Subagents** (`sub-agents.md`)
3. **Settings + Permissions** (`settings.md` + `permissions.md` + `permission-modes.md`)
4. **Memory / CLAUDE.md** (`memory.md` + `claude-directory.md`)

**Schwelle für Implementation:**
- ✅ **Autopilot** (Effort < 15 Min + Risiko 0): Permission ergaenzen, Hook-Format-Update, Tippfehler
- 🤔 **Praesentation an User** (Effort > 15 Min ODER Workflow-relevant): neuen Subagent definieren, Hierarchie aendern
- 🛑 **Strategie-Frage an User** (Architektur-Implikation): Plugin-System, Memory-Migration

---

## Aktueller Stand (Baseline)

**Project-Level `.claude/`** (D:\Projekte\cubetracker\\.claude\\):
- `settings.json` — Hooks-Config (5 Hooks: SessionStart, PreToolUse, PostToolUse Bash, PostToolUse Edit/Write, Stop)
- `commands/abschluss.md` — 1 Slash-Command (8-Punkte-QA-Routine)
- `hooks/` — 5 shell-Scripts:
  - `session-start-context.sh` (Repo-Stand-Dump)
  - `pre-bash-dev-server.sh` (Dev-Server-Schutz)
  - `post-git-commit.sh` (Patch-Notes-Erinnerung)
  - `post-edit-hardcoded-url.sh` (localhost-Bug-Schutz)
  - `stop-mini-check.sh` (Session-Ende-Backstop)
- `README.md` — Hook-Doku
- KEINE: `agents/`-Folder, plugins, output-styles, MCP-Configs

**User-Level `~/.claude/`** (C:\Users\Nutzer\.claude\):
- `settings.json` — eine Permission (`Bash(curl * https://ntfy.sh/*)`)
- `projects/.../memory/MEMORY.md` — Cross-Projekt-Memory-Index

**`CLAUDE.md`** (Project-Root):
- Disziplin: Edit statt Write, Type-Hints, Tests, Modul-Check, QA-Pflicht
- Konventionen: Patch-Notes-Format, Tag-Konvention, Branching
- Lessons aus 2026-05-16: Buffer-Polyfill, Bash-Heredoc-Quoting

---

## Sub-Agent-Reports

### 1. Hooks

**Was sagt die Doku?**

Hooks sind deterministische Shell-Commands an Lifecycle-Punkten (SessionStart, PreToolUse, PostToolUse, Stop, PermissionRequest, PostToolUseFailure, CwdChanged, FileChanged). Matcher + optionale `if:`-Conditions filtern. JSON-Decisions möglich (deny, additionalContext, permissionDecision). Best-Practices: kurze Timeouts (5-10s), breite Matcher + enge if-Conditions, Exit 0 = OK, Exit 2 = Block.

**Was machen wir aktuell?**

5 aktive Hooks, alle in `.claude/settings.json` registriert, 4 Events genutzt:

| Event | Matcher | Hook | Zweck |
|---|---|---|---|
| SessionStart | startup\|resume | `session-start-context.sh` | Repo-Stand-Dump |
| PreToolUse | Bash | `pre-bash-dev-server.sh` | Block lokale Dev-Server (mit Override-Env-Var) |
| PostToolUse | Bash + `git commit *` | `post-git-commit.sh` | Patch-Notes-Tag-Reminder |
| PostToolUse | Edit\|Write\|MultiEdit | `post-edit-hardcoded-url.sh` | Hardcoded-localhost-Schutz |
| Stop | (alle) | `stop-mini-check.sh` | Session-Ende-Backstop |

**Gap-Analyse:**
- ✅ Gut: zielgerichtet auf echte Projekt-Probleme, breite Matcher mit engen if-Conditions, alle hooks robust (kein jq-Dep)
- ⚠ Ungenutzt: **PermissionRequest** (Auto-Approve sicherer Commands), **PostToolUseFailure** (Diagnose bei git-push-Fail), **CwdChanged** (Repo-Status-Refresh bei /webapp ↔ /backend)
- ⚠ pre-bash-dev-server-Pattern `*uvicorn*` blockt auch `pip install uvicorn` (False Positive bei Dep-Installation)

**Vorschläge:**

| # | Vorschlag | Effort | Risiko | Schwelle |
|---|---|---|---|---|
| H1 | PermissionRequest-Hook für safe commands (git push/status/log, npm test/build) — reduziert Permission-Dialog-Fatigue | 10 Min | Niedrig | ✅ Autopilot |
| H2 | PostToolUseFailure-Hook für git-push-Diagnose (non-fast-forward, SSH-Fehler) | 15 Min | Niedrig | 🤔 Präsentation |
| H3 | README.md-Klarstellung dass `*uvicorn*`-Pattern auch `pip install uvicorn` blockt | 5 Min | 0 | ✅ Autopilot |

---

### 2. Subagents

**Was sagt die Doku?**

Subagents sind spezialisierte AI-Assistenten via Markdown-Files mit YAML-Frontmatter unter `.claude/agents/` (Project) oder `~/.claude/agents/` (User). Eigener Context, eigener System-Prompt, spezifischer Tool-Zugang, eigene Permissions. Custom Subagents lohnen wenn man identische Worker-Instruktion wiederholt spawnt (Reproduzierbarkeit + Versionierung + Token-Optimierung via Haiku-Model).

**Was machen wir aktuell?**

- **Built-in Subagents via `Agent`-Tool:** general-purpose, claude-code-guide, Explore, Plan — ad-hoc aufgerufen.
- **Beispiele aus Workflow:** QA-Reviews nach API-Endpoints, csTimer-Doku-Recherche, Plan-Phasen.
- **Gap im CLAUDE.md:** Sektion "Sub-Agent-Nutzung" (Zeile 82-92) empfiehlt code-writer / test-writer / reviewer / doc-writer — aber **keine Files unter `.claude/agents/` existieren**. Empfehlung ohne Implementation.

**Gap-Analyse:**
- Jedes QA-Review nutzt identischen Ad-hoc-Prompt → wäre als Subagent kapselbar
- Reproduzierbarkeit (versioniert, im Repo) + Token-Optimierung (Haiku statt Sonnet)
- 3 klare Kandidaten zur Materialisierung

**Vorschläge:**

| # | Subagent-File | Use-Case | Effort | Risiko | Schwelle |
|---|---|---|---|---|---|
| S1 | `.claude/agents/qa-reviewer.md` | Strukturierte QA-Reviews (KRITISCH/SOLLTE/NICE/POSITIV) — ersetzt aktuell wiederholten Ad-hoc-Prompt aus CLAUDE.md:94-133 | 10 Min | 0 | ✅ Autopilot |
| S2 | `.claude/agents/patch-notes-writer.md` | Schreibt PatchNote-Eintrag aus Commit-Diff (Format `2.0.0-alpha.W.X` deutsch) | 15 Min | 0 | 🤔 Präsentation |
| S3 | `.claude/agents/audit-loop.md` | Zirkulärer Doku-vs-Setup-Audit (= das was wir gerade machen) | 30 Min | Niedrig | 🛑 Strategie |

---

### 3. Settings + Permissions

**Was sagt die Doku?**

4-stufige Settings-Hierarchie: Managed > Local (`.claude/settings.local.json`) > Project (`.claude/settings.json`) > User (`~/.claude/settings.json`). Permission-Regeln: `Tool(specifier)` mit Wildcards (`*`/`**`). Evaluation: **Deny gewinnt immer** → Ask → Allow. Permission-Modes: default/acceptEdits/plan/auto/bypassPermissions. Env-Vars in `settings.json` unter `env`-Key.

**Was machen wir aktuell?**

| Ebene | File | Inhalt |
|---|---|---|
| Project | `.claude/settings.json` | Nur Hook-Config, KEINE Permissions |
| User | `~/.claude/settings.json` | 1 allow: `Bash(curl * https://ntfy.sh/*)` |
| Mode | default | Prompt bei jedem Tool-Call |
| Env-Vars | keine | Standard-Timeouts (120s Bash, 600s API) |

**Bekannte Pain-Points:**
- 2× pre-commit-Tag-Falle (Tag gepusht vor Commit-durch)
- 4× Auto-Mode-Classifier-Blocks: gh api, npm test, ntfy (gefixt), bulk-rm, self-modify
- Plan-Mode nie genutzt obwohl Major-Refactors häufig

**Gap-Analyse:**
- **Allow-List leer im Project** — jeder wiederkehrende Bash-Call triggert Classifier
- **Deny-List komplett fehlt** — kein Safety-Net gegen `rm -rf /*` etc.
- **Plan-Mode-Strategie undokumentiert** — User weiss nicht wann Shift+Tab nutzen
- **Env-Vars ungenutzt** — kein Debug-Logging, kein verlängertes Timeout für Python-Heavy

**Vorschläge:**

| # | Vorschlag | Effort | Risiko | Schwelle |
|---|---|---|---|---|
| P1 | Project-Allow-List: gh api repos/cs0x7f/*, npm test/run, python -c, git tag, find -delete | 5 Min | 0 | ✅ Autopilot |
| P2 | Project-Deny-List: rm -rf /*, git push --force* main, Edit(.git/**) | 10 Min | 0 | ✅ Autopilot |
| P3 | Pre-Tag-Hook gegen misplaced-Tag-Falle (checks dass letzter Commit clean ist) | 15 Min | Niedrig | 🤔 Präsentation |
| P4 | Env-Vars: BASH_DEFAULT_TIMEOUT_MS=180000 für Python-Heavy | 3 Min | 0 | ✅ Autopilot |
| P5 | CLAUDE.md: Permission-Modes-Sektion + Shift+Tab-Shortcut dokumentieren | 5 Min | 0 | ✅ Autopilot |

---

### 4. Memory / CLAUDE.md

**Was sagt die Doku?**

CLAUDE.md (~200 Zeilen optimal, bei Session-Start geladen) + Auto Memory (Claude-selbst, max 25KB aus `MEMORY.md`). `@`-Imports werden bei Launch expandiert. Path-scoped Rules in `.claude/rules/*.md` (mit YAML `paths:`) laden nur bei File-Matches → context-sparend. User-Memory `~/.claude/` ist Cross-Projekt; Project-Memory ist Team-shared.

**Was machen wir aktuell?**

| File | Zeilen | Update |
|---|---|---|
| `CLAUDE.md` | 227 | Manuell, wächst unbounded |
| `NEXT_SESSION.md` | ~540 | Manuell am Session-Ende |
| `.claude/commands/abschluss.md` | 152 | On-Demand |
| `MEMORY.md` (User) | klein | Cross-Projekt-Index |

**Gap-Analyse:**
- **CLAUDE.md überlang** (227 > 200 empfohlen) — Lessons-Sektion wächst unbounded
- **@-Imports ungenutzt** — Disziplin-Block (40 Zeilen) könnte in Path-scoped Rule ziehen
- **Lessons-Drift** — alte Events (cstimer-Bug 2026-05-16, Bash-Heredoc) sollten archiviert nach >21 Tagen
- **NEXT_SESSION-Update fragmentiert** — manuell + abschluss-Erinnerung, kein Hard-Hook
- **Single-Source-Files nicht dokumentiert** — User weiss nicht welche Files canonical sind

**Vorschläge:**

| # | Vorschlag | Effort | Risiko | Schwelle |
|---|---|---|---|---|
| M1 | Disziplin-Block → `.claude/rules/discipline.md` mit `paths: ["**/*.py", "**/*.ts", "**/*.tsx"]` — CLAUDE.md auf ~120 Zeilen | 10 Min | 0 | ✅ Autopilot |
| M2 | Lessons-Sektion → `docs/lessons-archive.md` — CLAUDE.md auf ~100 Zeilen | 5 Min | 0 | ✅ Autopilot |
| M3 | Single-Source-Files-Sektion in NEXT_SESSION.md Kopf | 3 Min | 0 | ✅ Autopilot |
| M4 | NEXT_SESSION.md-Update-Hook (post-git-push aktualisiert Header) ODER abschluss-Skill-Prompt | 15 Min | Niedrig | 🤔 Präsentation |

---

## Synthese

### Quick-Wins (Autopilot, alle <15 Min + Risiko 0)

**~67 Min Gesamt-Effort, ein Sammel-Commit:**

| # | Was | Effort | Source |
|---|---|---|---|
| 1 | P1: Project-Allow-List (gh, npm, python, git tag) | 5 Min | Settings |
| 2 | P2: Project-Deny-List (rm -rf, force-push main) | 10 Min | Settings |
| 3 | P4: Env-Vars für längere Timeouts | 3 Min | Settings |
| 4 | P5: CLAUDE.md Permission-Modes-Sektion | 5 Min | Settings |
| 5 | H1: PermissionRequest-Hook für safe commands | 10 Min | Hooks |
| 6 | H3: README.md pre-bash-dev-server Klarstellung | 5 Min | Hooks |
| 7 | M1: Disziplin → `.claude/rules/discipline.md` | 10 Min | Memory |
| 8 | M2: Lessons → `docs/lessons-archive.md` | 5 Min | Memory |
| 9 | M3: Single-Source-Files in NEXT_SESSION.md | 3 Min | Memory |
| 10 | S1: `.claude/agents/qa-reviewer.md` materialisieren | 10 Min | Subagents |

### Präsentation an User (>15 Min ODER Workflow-relevant)

| # | Was | Effort | Risiko | Wert |
|---|---|---|---|---|
| H2 | PostToolUseFailure-Hook für git-push-Diagnose | 15 Min | Niedrig | Selten genutzt, aber wertvoll bei Merge-Konflikten |
| P3 | Pre-Tag-Hook gegen misplaced-Tag-Falle | 15 Min | Niedrig | **2× erlebt, hoher Wert** |
| S2 | patch-notes-writer Subagent | 15 Min | 0 | Mittel — wir schreiben Patch-Notes oft, aber nicht trivial automatisierbar |
| M4 | NEXT_SESSION-Update-Hook | 15 Min | Niedrig | Würde Disziplin erzwingen, aber könnte lästig sein |

### Strategie-Items (User-Entscheidung)

| # | Was | Effort | Charakter |
|---|---|---|---|
| S3 | audit-loop Subagent für wiederkehrende Audits | 30 Min | Selbst-reflexiv — Cubetracker wartet sich selbst |

---

## Implementations-Log

### Quick-Wins (Phase D Autopilot, 2026-05-20)

Alle 10 Items in einem Sammel-Commit:

| # | Item | Status | File(s) |
|---|---|---|---|
| 1 | P1: Project-Allow-List (gh api/cs0x7f+fischehaus, npm test/build/lint, python -c, git tag/log/status/diff/push, find -delete) | ✅ done | `.claude/settings.json` |
| 2 | P2: Project-Deny-List (rm -rf /, force-push main, .git-Edits) | ✅ done | `.claude/settings.json` |
| 3 | P4: Env-Var `BASH_DEFAULT_TIMEOUT_MS=180000` | ✅ done | `.claude/settings.json` |
| 4 | P5: CLAUDE.md Permission-Modes-Sektion + Shift+Tab dokumentiert | ✅ done | `CLAUDE.md` |
| 5 | H1: PermissionRequest-Hook für safe commands | ✅ done | `.claude/hooks/permission-request-auto-approve.sh` (neu) + `.claude/settings.json` |
| 6 | H3: README.md pre-bash-dev-server Klarstellung | ✅ done | `.claude/README.md` |
| 7 | M1: Disziplin-Block → `.claude/rules/discipline.md` (path-scoped) | ✅ done | `.claude/rules/discipline.md` (neu) + `CLAUDE.md` schrumpft |
| 8 | M2: Lessons-Sektion → `docs/lessons-archive.md` | ✅ done | `docs/lessons-archive.md` (neu) + `CLAUDE.md` schrumpft |
| 9 | M3: Single-Source-Files-Sektion in NEXT_SESSION.md Kopf | ✅ done | `NEXT_SESSION.md` |
| 10 | S1: `.claude/agents/qa-reviewer.md` materialisieren | ✅ done | `.claude/agents/qa-reviewer.md` (neu) |

**Stats:**
- 4 neue Files (rules/discipline.md, hooks/permission-request-auto-approve.sh, agents/qa-reviewer.md, lessons-archive.md)
- 5 modifizierte Files (settings.json, CLAUDE.md, README.md, NEXT_SESSION.md, dieses Audit-File)
- CLAUDE.md schrumpft von 227 auf ~165 Zeilen
- Tests 158/158 weiterhin grün (kein App-Code angefasst)
- 1 Bemerkenswerter Auto-Mode-Classifier-Block: Komplett-Write auf CLAUDE.md geblockt (Self-Modification-Schutz) → chirurgische Edits via Edit-Tool stattdessen.

### Präsentations-Items (Phase D, User-Auswahl 2026-05-20)

User hat 3 von 4 freigegeben (P3 + S2 + H2). Implementiert im 2. Sammel-Commit
`chore(W.setup-audit-phase-d)`:

| # | Item | Status | File(s) |
|---|---|---|---|
| P3 | Pre-Tag-Hook gegen misplaced-Tag-Falle — deny bei modifizierten tracked-Files vor `git tag` (Untracked egal, Override `CUBETRACKER_ALLOW_DIRTY_TAG=1`) | ✅ done | `.claude/hooks/pre-git-tag-check.sh` (neu) + `.claude/settings.json` (PreToolUse, if `Bash(git tag *)`) |
| H2 | PostToolUseFailure-Hook für git-push-Diagnose — non-fast-forward / no-upstream / auth / network / protected-branch + Fallback | ✅ done | `.claude/hooks/post-push-failure-diagnose.sh` (neu) + `.claude/settings.json` (neues `PostToolUseFailure`-Event, if `Bash(git push *)`) |
| S2 | patch-notes-writer Subagent — PatchNote-Eintrag aus Commit-Diff, deutsch, Format `2.0.0-alpha.W.X`, fügt oben in PATCH_NOTES ein | ✅ done | `.claude/agents/patch-notes-writer.md` (neu) |
| M4 | NEXT_SESSION-Update-Hook | ⏸ offen | User hat M4 nicht ausgewählt — bleibt zurückgestellt |

**Tests:** P3 6 Szenarien grün (clean→allow, dirty→deny, listing/delete/override/untracked-only→allow). H2 7 Szenarien grün (5 Fehler-Muster + unknown-Fallback + non-push-ignored). `settings.json` JSON-valide nach Registrierung.

**Bemerkenswert (Audit-relevant): 2× Auto-Mode-Classifier-Block** auf den
`settings.json`-Edits — Begründung "Self-Modification of agent config". Der 1.
Block kam, weil die Item-Freigabe aus dem geladenen Protokoll-Docx stammte (nicht
aus dem aktiven Chat); der 2. Block, weil eine AskUserQuestion-Antwort nicht als
ausformulierte Freigabe zählt. Erst eine ausgeschriebene User-Anweisung im
Chat-Text ("Ja, registriere die beiden Hooks…") hat den Edit durchgelassen. Die
Hook-Script-Files selbst (`.claude/hooks/*.sh`) + das Agent-File waren NICHT
geblockt — nur die `settings.json`-Registrierung. → **Lesson:** Self-Modify von
Agent-Startup-Config (settings.json) braucht eine textuelle In-Session-Freigabe;
geladene Tool-Inhalte + strukturierte Tool-Antworten reichen dem Classifier nicht.

### Strategie (Phase D, User-Entscheidung)

- **S3** `.claude/agents/audit-loop.md` — wiederkehrender Setup-Audit-Subagent (30 Min).
  **Zurückgestellt** (User 2026-05-20): „nicht jetzt — vielleicht später, wenn wir
  die anderen 9 Doku-Sektionen auch durch haben."

---

## Synthese
_(wird in Phase C ergaenzt)_

## Implementations-Log
_(wird in Phase D ergaenzt)_
